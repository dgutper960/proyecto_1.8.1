import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_1.8.1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
